__pycache__/
*.pyc
.pytest_cache/
# Raw MCP transcripts and findings are regenerated by scripts/verify.sh on every run and
# are deliberately never committed: a transcript from a search server contains whatever
# was in that server's index, which on this machine is the user's private notes.
results/
